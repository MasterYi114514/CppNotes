
# 1. 编译操作

## 1.1 可执行文件

**可执行文件（executable file）**：能被操作系统直接加载，让计算机执行的文件。

在 Windows 上，常见的可执行文件扩展名是 `.exe`，在 Linux 和 macOS 上则没有特定的扩展名，有时会使用 `.out`。

我们写的cpp代码是人类可以看懂的高级语言，却不是计算机能看懂的机器语言。因此我们需要**编译**操作，将cpp代码转换成可执行文件。

## 1.2 编译

**编译**：将源代码转换成可执行文件的过程。

C++文件的编译过程可以简单地分为以下几个步骤：
```
a.cpp(源代码) -> a.i(预处理后的代码) -> a.s(汇编代码) -> a.o(目标文件) -> a.exe(可执行文件)
```

我们会在后续的学习中逐步理解这里每个步骤的含义。

# 2. VScode的环境配置

# 2.0 几个概念的区分

**编辑器（editor）**：用于编写代码的软件，如 VisualStudioCode（VScode）等。
**编译器（compiler）**：将源代码转换成可执行文件的程序，如 g++、clang++ 等。
**集成开发环境（IDE）**：集成了编辑器、编译器和调试工具的开发环境，如 Visual Studio、Dev-C++、Code::Blocks 等。

# 2.1 VScode的安装

# 2.2 MinGW的安装与系统环境变量配置

MinGW是一个在Windows平台上使用的C/C++编译器，提供了GNU工具链的Windows版本。
 - **GNU工具链**：包括编译器（如 gcc、g++）、链接器、调试器等工具，用于开发C/C++程序。

# 3. 通过配置 tasks.json 实现在VScode上一键编译运行代码

# 3.1 JSON 文件的简单介绍

# 3.2 tasks.json 的配置

